{
  "name": "sql-limiter",
  "version": "3.0.0",
  "description": "Injects and enforces row limits in your SQL statements.",
  "keywords": [
    "sql",
    "tokenize",
    "lexer"
  ],
  "main": "src/index.js",
  "scripts": {
    "build-docs": "npm run build --prefix docs-src",
    "test": "mocha test --bail",
    "fixlint": "eslint --fix \"**/*.js\"",
    "lint": "eslint \"**/*.js\" && prettier --check .",
    "prepublishOnly": "npm test && npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sqlpad/sql-limiter.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sqlpad/sql-limiter/issues"
  },
  "homepage": "https://github.com/sqlpad/sql-limiter#readme",
  "dependencies": {
    "moo": "^0.5.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^8.10.0",
    "mocha": "^9.2.2",
    "prettier": "^2.8.8"
  }
}
